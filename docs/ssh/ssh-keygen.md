# Creating a Key pair

```bash
 ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

```bash
 ssh-keygen -t rsa -b 4096 -C "your_email@example.com" -f ~/.ssh/name_of_your_key
```


## Futher reading

https://www.ssh.com/ssh/keygen/

